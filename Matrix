package data_day5;

public class Matrix {
	public static void main(String args[]) {
		int sale[]=new int[] {157,209,251,312};
		
		for(int i=0;i<sale.length;i++) {
			System.out.printf("%d/4분기 : sale[%d]= %d %n",
					i+1, i, sale[i]);//%d 는 해당위치에 숫자 넣음
			//%n은 하나씩 출력할때마다 줄바꿈
		}
	}
}
